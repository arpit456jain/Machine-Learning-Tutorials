name: "PR Greeter"
on: [pull_request, issues]

jobs:
  pr-greeter:
    runs-on: ubuntu-latest
    steps:
      - name: "Greeter"
        uses: JJ/pr-greeting-action@releases/v1
        with:
         repo-token: ${{ secrets.GITHUB_TOKEN }}
         issue-message: 'Thanks for opening an issue waiting for a contributor like you😁😁'
         pr-message: 'Thanks for opening a PR I will review it soon 😃😃'

